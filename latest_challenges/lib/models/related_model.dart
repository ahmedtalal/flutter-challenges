class RelatedModel {
  final String name, description, image;
  final double rating, salary;
  final int startTime, endTime;
  const RelatedModel({
    required this.name,
    required this.description,
    required this.salary,
    required this.rating,
    required this.startTime,
    required this.endTime,
    required this.image,
  });
}
